{
  "name": "peage",
  "version": "1.0.0",
  "private": true,
  "description": "Modern URL Shortener with Monetization",
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "turbo dev --filter=@peage/api",
    "dev:web": "turbo dev --filter=@peage/web",
    "build": "turbo build",
    "deploy": "turbo deploy",
    "deploy:staging": "turbo deploy -- --env staging",
    "deploy:production": "turbo deploy -- --env production",
    "test": "cd apps/api && npx vitest run --passWithNoTests || true",
    "test:coverage": "cd apps/api && npx vitest run --coverage --passWithNoTests || true",
    "lint": "eslint apps/api/src/ --ext .ts || true",
    "lint:fix": "eslint apps/api/src/ --ext .ts --fix || true",
    "typecheck": "turbo typecheck || true",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:studio": "turbo db:studio",
    "db:seed": "turbo db:seed",
    "prepare": "husky install || true",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "husky": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
